{
  "name": "nostr-nip72-poller",
  "version": "1.0.0",
  "description": "Cloudflare Worker for polling NIP-72 Nostr relays and dispatching push notifications",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "test": "vitest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "@types/node": "^22.5.5",
    "typescript": "^5.6.2",
    "vitest": "^2.0.5",
    "wrangler": "^3.78.2"
  },
  "dependencies": {
    "nostr-tools": "^2.7.2"
  }
}
